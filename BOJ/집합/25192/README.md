문제 조건 생각해보면, 아래와 같다.
1. ENTER 아니면 
2. 똑같은 애 아니면
3. ENTER 다시 입력하면

해결 방법은 아래와 같다.
1. ENTER 아니면 -> if 문
2. 똑같은 애 아니면 -> set으로 자동으로 해결
3. ENTER 다시 입력하면-> set이니깐 clear하면 됨.

set 자료구조를 많이 안 써봐서 헷갈렸다.
추가할 때는 push_back이 아니라, insert()였다.
초기화는 clear()였다.
